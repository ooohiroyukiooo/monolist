 <%#= link_to "   " , ownerships_path(asin: item.get('ASIN') , type: "Haved") ,
                      :class => "have-#{item.get('ASIN')} btn-have btn btn-warning btn-sm have",
                      :remote => true,
                      :method => :post %>
<%= link_to "" , 
  ownership_path( current_user.haves.find_by(:item_id => item.id) , 
    item_id: item.id , type: "Have") ,
  :class => "have-#{item.id} btn-have btn btn-warning btn-sm haved",
  :remote => true,
  :method => :delete %> 
